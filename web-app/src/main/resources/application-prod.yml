spring:
  thymeleaf:
    cache: true

canineai:
  backend:
    base-url: http://backend:8080/api/v1
    timeout-ms: 10000

logging:
  level:
    org.springframework.web: INFO
    com.canineai.webapp: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
